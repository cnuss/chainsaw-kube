apiVersion: v1
kind: Pod
metadata:
  name: node-selector-match
  labels:
    app: node-selector-match
spec:
  nodeSelector:
    kubernetes.io/os: linux
  containers:
    - name: nginx
      image: nginx:1.27
