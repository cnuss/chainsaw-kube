apiVersion: v1
kind: Pod
metadata:
  name: node-selector-nomatch
  labels:
    app: node-selector-nomatch
spec:
  nodeSelector:
    non-existent-label: no-such-value
  containers:
    - name: nginx
      image: nginx:1.27
