apiVersion: kuttl.dev/v1beta1
kind: TestAssert
timeout: 30
commands:
  - script: |
      set -e
      status=$(kubectl get namespace kuttl-ns-test-${NAMESPACE##*-} -o jsonpath='{.status.phase}')
      test "$status" = "Active"
