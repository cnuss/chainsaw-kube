apiVersion: v1
kind: Pod
metadata:
  name: affinity-pod
  labels:
    app: affinity-pod
spec:
  affinity:
    podAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        - labelSelector:
            matchExpressions:
              - key: app
                operator: In
                values:
                  - target
          topologyKey: kubernetes.io/hostname
  containers:
    - name: nginx
      image: nginx:1.27
