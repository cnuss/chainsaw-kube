apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-recreate
spec:
  template:
    spec:
      containers:
        - name: nginx
          image: nginx:1.27-alpine
          ports:
            - containerPort: 80
