apiVersion: v1
kind: Pod
metadata:
  name: restart-never
  labels:
    app: restart-never
spec:
  restartPolicy: Never
  containers:
    - name: busybox
      image: busybox:1.37
      command: ["sh", "-c", "echo done && exit 0"]
