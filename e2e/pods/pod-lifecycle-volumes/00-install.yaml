apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 64Mi
---
apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    app: nginx
spec:
  containers:
    - name: nginx
      image: nginx:1.27
      ports:
        - containerPort: 80
      lifecycle:
        postStart:
          exec:
            command: ["sh", "-c", "date | tee -a /pvc/started.txt >> /scratch/started.txt"]
      volumeMounts:
        - name: pvc
          mountPath: /pvc
        - name: scratch
          mountPath: /scratch
  volumes:
    - name: pvc
      persistentVolumeClaim:
        claimName: test-pvc
    - name: scratch
      emptyDir: {}
